import 'package:flutter/material.dart';

class MechanicService extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: MechanicServiceBody(),
    );
  }
}

class MechanicServiceBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
