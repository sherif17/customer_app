import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class EnterPhoneNumber extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: Center(
        child: Text(
          'Please Enter Your Phone Number Page',
        ),
      ),
    );
  }
}
